diff --git a/api/main.py b/api/main.py
index 0d87400..a324fe3 100644
--- a/api/main.py
+++ b/api/main.py
@@ -1,4 +1,4 @@
-from fastapi import FastAPI
+from fastapi import FastAPI, HTTPException, Query
 from fastapi.middleware.cors import CORSMiddleware
 from fastapi.staticfiles import StaticFiles
 from fastapi.responses import FileResponse
@@ -35,7 +35,10 @@ def api_info():
 
 
 @app.get("/api/quote")
-def generate_quote():
-    quote = get_quote()
+def generate_quote(category: str | None = Query(default=None)):
+    try:
+        quote = get_quote(category=category)
+    except ValueError:
+        raise HTTPException(status_code=404, detail="No quotes found for that category.")
     return {"quote": quote}
 
diff --git a/engines/quote_engine.py b/engines/quote_engine.py
index 55506cb..b866860 100644
--- a/engines/quote_engine.py
+++ b/engines/quote_engine.py
@@ -3,21 +3,26 @@ import random
 import os
 
 
-def generate_quote() -> str:
+def generate_quote(category: str | None = None) -> str:
     """
     Generates a random quote from quotes_simplified.json.
-    
+
     Returns:
         str: A formatted string in the format "This is a quote about {Category} by {Author}: {Quote}."
     """
     # Get the directory where this script is located
     script_dir = os.path.dirname(os.path.abspath(__file__))
     json_path = os.path.join(script_dir, 'quotes_simplified.json')
-    
+
     # Load the quotes from JSON file
     with open(json_path, 'r', encoding='utf-8') as f:
         quotes = json.load(f)
-    
+
+    if category is not None:
+        quotes = [q for q in quotes if q.get('Category', '').lower() == category.lower()]
+        if not quotes:
+            raise ValueError(f"No quotes found for category: {category}")
+
     # Select a random quote
     quote_data = random.choice(quotes)
     
diff --git a/tests/test_category_filter.py b/tests/test_category_filter.py
new file mode 100644
index 0000000..43b9c96
--- /dev/null
+++ b/tests/test_category_filter.py
@@ -0,0 +1,31 @@
+import sys
+import os
+
+sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
+
+from starlette.testclient import TestClient
+from api.main import app
+
+client = TestClient(app)
+
+
+def test_no_category_returns_quote():
+    response = client.get("/api/quote")
+    assert response.status_code == 200
+    data = response.json()
+    assert "quote" in data
+    assert data["quote"]
+
+
+def test_category_life_returns_quote():
+    response = client.get("/api/quote", params={"category": "life"})
+    assert response.status_code == 200
+    data = response.json()
+    assert "quote" in data
+    assert data["quote"]
+
+
+def test_nonexistent_category_returns_404():
+    response = client.get("/api/quote", params={"category": "nonexistent_xyz"})
+    assert response.status_code == 404
+    assert response.json()["detail"] == "No quotes found for that category."
